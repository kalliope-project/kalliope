---
  - name: "run-simple-script"
    signals:
      - order: "lance le script"
    neurons:
      - script:
          path: "/var/tmp/coucou.sh"
      - say:
          message: "Script lancé, monsieur"
