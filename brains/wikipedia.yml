---

  - name: "wikipedia-search"
    neurons:
      - wikipedia:
          language: "fr"
          args:
            - query
          file_template: "wikipedia_returned_value.j2"
    signals:
      - order: "cherche sur Wikipédia {{ query }}"
